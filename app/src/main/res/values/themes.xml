<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Vibe" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/my_light_secondary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="backgroundColor">@color/my_light_background</item>
        <item name="colorOnError">@color/my_light_error</item>
        <item name="colorOnPrimary">@color/my_light_onPrimary</item>
        <item name="cardBackgroundColor">@color/cardViewLightBackground</item>

    </style>

    <style name="Theme.Vibe" parent="Base.Theme.Vibe" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/my_dark_background_two</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/bg</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Vibe</item>
    </style>

</resources>